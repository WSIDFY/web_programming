function over(obj)  // this 사용시 오류 발생으로 인해 매개변수인 obj를 통해 Dom요소에 접근
{
    obj.style.background="yellow";  // this 사용시 오류 발생으로 인해 매개변수인 obj를 통해 Dom요소에 접근
}
function out(obj)
{
  obj.style.background="white";
}
